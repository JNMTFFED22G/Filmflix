export default function HomePage() {
  return (
    <div>
      <h1>HomePage</h1>
      {/* {movies.map((movie, index) => (
        <Thumbnail movie={movie} mode='rec' key={index} />
      ))} */}
    </div>
  );
}
